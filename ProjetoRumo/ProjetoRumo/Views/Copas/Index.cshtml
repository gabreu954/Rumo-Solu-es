@model IEnumerable<ProjetoRumo.Models.Copa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bebida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bebida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCopa }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCopa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCopa })
        </td>
    </tr>
}

</table>
